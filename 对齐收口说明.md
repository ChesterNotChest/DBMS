# 对齐收口说明

本文用于把组员侧的 parser + 前端原型，收口到当前 DBMS 项目的 service/repo 架构中。目标不是照搬实现，而是按方法分工对齐职责，保证后续可以稳定合并、联调和验收。

## 一、总体约束

1. parser 只负责识别 SQL、拆解参数、生成命令结果，不直接操作文件系统。
2. display 只负责界面输入、结果展示、结构树刷新，不直接写业务逻辑。
3. executor / dispatcher 负责把 parser 结果映射到当前项目的 service 接口。
4. 当前项目的真实业务入口只保留在 service/repo 层，不允许前端绕过 service 直连 repo。
5. currentDatabase、dataRoot 统一由 service 层管理，UI 只读取状态，不自己维护业务状态。

## 二、建议目录收口

### 1. parser 层

- utils/sql_parser/database_parser.cpp
- utils/sql_parser/table_parser.cpp
- utils/sql_parser/tuple_parser.cpp

这三份文件只做 SQL 解析和命令对象拆分，不做界面，不做文件读写，不做 repo 调用。

### 2. display 层

- display/editor_panel.cpp
- display/result_panel.cpp
- display/structure_panel.cpp

这三份文件只做 Qt 界面，不做 SQL 解析，不直接访问文件，不直接操作 repo。

### 3. 执行层

建议新增一个中间层，例如：

- utils/sql_executor/sql_executor.cpp
- 或者 service/sql_dispatcher.cpp

职责是把 parser 产出的命令对象，转换为当前项目的 service 调用。

## 三、按方法对齐

### A. 数据库级方法

| SQL 方法 | parser 负责什么 | 对应 service 调用 | UI 结果 |
|---|---|---|---|
| CREATE DATABASE | 提取数据库名 | database_service::createDatabase | result panel 显示成功/失败 |
| DROP DATABASE | 提取数据库名 | database_service::dropDatabase | result panel 显示成功/失败，必要时刷新结构树 |
| USE DATABASE | 提取数据库名 | database_service::useDatabase | 成功后刷新 structure panel |
| SHOW DATABASES | 无参数或简单参数 | database_service::showDatabases | result panel 显示数据库列表 |

### B. 表级方法

| SQL 方法 | parser 负责什么 | 对应 service 调用 | UI 结果 |
|---|---|---|---|
| CREATE TABLE | 提取表名、列定义、约束定义 | table_service::createTable | 成功后刷新结构树 |
| DROP TABLE | 提取表名 | table_service::dropTable | 成功后刷新结构树 |
| ALTER TABLE ADD COLUMN | 提取表名、列定义 | table_service::addColumn | 成功后刷新结构树和结果区 |
| ALTER TABLE DELETE COLUMN | 提取表名、列名 | table_service::deleteColumn | 成功后刷新结构树和结果区 |
| ALTER TABLE MODIFY COLUMN | 提取表名、列名、新定义 | table_service::modifyColumn | 成功后刷新结构树和结果区 |
| ALTER TABLE ADD CONSTRAINT | 提取表名、约束定义 | table_service::addConstraint | 成功后刷新结构树和结果区 |
| ALTER TABLE MODIFY CONSTRAINT | 提取表名、约束名、新定义 | table_service::modifyConstraint | 成功后刷新结构树和结果区 |
| ALTER TABLE DELETE CONSTRAINT | 提取表名、约束名 | table_service::deleteConstraint | 成功后刷新结构树和结果区 |
| SHOW TABLES | 无参数或当前数据库参数 | table_service::showTables | result panel 显示表列表 |
| DESC 表名 | 提取表名 | table_service::describeTable | result panel 显示表结构 |
| SHOW CREATE TABLE 表名 | 提取表名 | table_service::showCreateTable | result panel 显示建表语句 |

### C. 元组级方法

| SQL 方法 | parser 负责什么 | 对应 service 调用 | UI 结果 |
|---|---|---|---|
| SELECT | 提取投影列、表名、条件 | tuple_service::selectRows | result panel 显示查询结果 |
| INSERT | 提取表名、行数据 | tuple_service::insertRows | result panel 显示影响行数 |
| UPDATE | 提取表名、赋值表达式、条件 | tuple_service::updateRows | result panel 显示影响行数 |
| DELETE | 提取表名、条件 | tuple_service::deleteRows | result panel 显示影响行数 |

## 四、前端收口职责

### mainwindow

整个窗口统一用 `mainwindow` 封装，作为主容器承载 editor_panel、result_panel、structure_panel。

要求：

- 主窗口只做整体布局和信号转发，不单独拆成多个顶层窗口。
- 所有面板都作为 mainwindow 的子组件管理，便于统一刷新、统一状态同步和统一关闭。
- 后续如果扩展菜单栏、工具栏、标签页，也优先放在 mainwindow 内部完成。

### editor_panel

建议只保留以下能力：

- 输入 SQL
- 执行当前 SQL
- 清空输入
- 运行脚本

不要把解析逻辑写到 editor 里，也不要让 editor 直接调用 repo。

### result_panel

建议只保留以下能力：

- 显示查询表格
- 显示执行日志
- 显示错误信息
- 统一格式化 service 返回结果

### structure_panel

建议只保留以下能力：

- 左侧树形展示 database -> table -> column
- 展示当前数据库状态
- 点击 database 节点后自动执行 USE
- 成功执行 CREATE / DROP / ALTER 后刷新树

## 五、必须统一的命名和状态

1. 当前数据库状态统一使用 service::currentDatabase。
2. 数据根目录统一使用 service::currentDataRoot。
3. parser 侧不要自己再维护一份数据库状态。
4. 所有 SQL 命令对象尽量统一返回 success、errorMessage、payload 三类信息，便于 UI 直接展示。

## 六、建议的收口顺序

1. 先收口 parser，只保证数据库级、表级、元组级三类 SQL 能稳定识别。
2. 再接 executor，把 parser 结果转成 service 调用。
3. 再接 display，把输入、结果、结构树串起来。
4. 最后补联调，逐条验证 CREATE、DROP、USE、SHOW、SELECT、INSERT、UPDATE、DELETE。

## 七、验收标准

1. parser 不直接碰文件系统。
2. display 不直接碰 repo。
3. 所有业务操作都走当前项目 service 层。
4. USE 成功后，结构树和后续表操作都能正确识别当前数据库。
5. CREATE / DROP / ALTER 后，结构树能刷新到最新状态。
6. SELECT / INSERT / UPDATE / DELETE 的结果能正确回显到 result panel。

## 八、给对方的最小执行清单

1. 按 database_parser、table_parser、tuple_parser 三个文件拆分 parser。
2. 按 editor_panel、result_panel、structure_panel 拆分界面。
3. 补一个 executor / dispatcher 中间层，专门对接当前项目 service。
4. 不要把 main.cpp 写成一体化业务实现。
5. 不要让前端直接访问 repo 或文件目录。

## 九、备注

如果后续要继续扩展 JOIN、子查询、GROUP BY、HAVING、ORDER BY，这些应当继续留在 parser 层做语法拆解，但业务执行仍然必须落到 service 层，不建议在 display 中增加任何 SQL 语义处理。